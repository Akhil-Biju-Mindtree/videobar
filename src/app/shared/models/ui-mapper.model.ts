export class UIMapperModel {
  uuid: string;
  group: string;
  name: string;
  actions: string;
  canonical_name: string;
  data_type: string;
  regex_values: string;
  default: string;
  label: string;
  range_min: string;
  range_max: string;
  range_step: string;
  reboot: string;
  api_version: string;
  access: string;
  disk_stored: string;
  provision: string;
  ui_widget: string;
  command_id: string;
  payload?: {};
}
